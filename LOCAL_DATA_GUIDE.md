# 📊 本地數據版 - 使用指南

## 升級完成！ ✅

系統已經**完全轉換為本地數據模式**，不再依賴 Yahoo Finance API：

- **後端服務器**: 運行在 `http://localhost:8080`
- **數據來源**: 本地 `stock_data.json` 文件
- **數據管理工具**: 可在 `http://localhost:8081` 訪問

---

## 系統架構

### 1️⃣ 主預測儀表板 (Port 8080)
```
http://localhost:8080
```
- 顯示股票預測
- 自動每 10 秒更新一次
- 所有數據來自本地存儲

### 2️⃣ 數據管理工具 (Port 8081)
```
http://localhost:8081
```
- 修改股票價格
- 更新技術指標（MA、RSI、波動率等）
- 即時保存到 `stock_data.json`

---

## 如何更新股票價格

### 方式 1: 使用數據管理工具（推薦）

1. **啟動數據管理工具**:
   ```bash
   python data_manager.py
   ```

2. **打開瀏覽器**: 訪問 `http://localhost:8081`

3. **編輯價格**:
   - 找到要編輯的股票卡片
   - 修改「當前價格」欄位
   - 點擊「💾 保存」按鈕
   - 看到綠色提示表示保存成功

4. **馬上生效**:
   - 不需要重啟服務器
   - 主儀表板會自動使用新數據
   - 刷新 `http://localhost:8080` 即可看到更新

### 方式 2: 直接編輯 JSON

1. **打開** `stock_data.json`
2. **找到要改的股票**:
   ```json
   {
     "code": "8299",
     "name": "群聯",
     "current_price": 2340.00,
     "day_change_pct": 1.30,
     ...
   }
   ```
3. **修改價格**:
   - 改變 `current_price` 值
   - 改變 `day_change_pct` 值（可選）
4. **保存檔案**
5. **刷新瀏覽器** - 新數據立即生效

---

## 完整的數據欄位說明

每隻股票需要以下欄位：

| 欄位 | 說明 | 例子 |
|------|------|------|
| `code` | 股票代碼 | `"8299"` |
| `name` | 股票名稱 | `"群聯"` |
| `current_price` | 當前價格 (NT$) | `2340.00` |
| `day_change_pct` | 漲跌幅 (%) | `1.30` |
| `rsi` | RSI 指標 (0-100) | `56.31` |
| `ma5` | 5日均線 | `2459.0` |
| `ma10` | 10日均線 | `2416.0` |
| `ma20` | 20日均線 | `2450.25` |
| `ma50` | 50日均線 | `2309.1` |
| `ma200` | 200日均線 | `1566.19` |
| `support` | 支撐價位 | `2273.57` |
| `resistance` | 壓力價位 | `2683.35` |
| `volatility` | 波動率 (%) | `2.85` |

---

## 常見問題

### Q: 我想增加新股票怎麼辦？

**方式 1 - 編輯 JSON**:
在 `stock_data.json` 的 `stocks` 陣列中添加新對象:
```json
{
  "code": "新代碼",
  "name": "新股票名稱",
  "current_price": 價格,
  "day_change_pct": 漲跌幅,
  ...其他欄位...
}
```

**方式 2 - 使用管理工具**:
(功能計劃中 - 目前需手動編輯)

### Q: 為什麼資料不會自動更新？

因為現在是本地存儲模式，**您需要手動更新價格**：
- 使用數據管理工具 (`http://localhost:8081`)
- 或直接編輯 `stock_data.json`

### Q: 我可以離線使用嗎？

**是的！** 這是本地數據版本的好處：
- 無須網路連接
- 無須 Yahoo Finance API
- 完全獨立、可靠運行

### Q: 如何備份數據？

1. **複製 `stock_data.json` 檔案**到安全地點
2. **定期備份** - 建議每天備份一次

---

## 啟動指令

**啟動主預測儀表板**:
```bash
cd c:\Users\USER\Desktop\taiwan-stock-predictor
python simple_app.py
# 訪問: http://localhost:8080
```

**啟動數據管理工具**（另開終端）:
```bash
cd c:\Users\USER\Desktop\taiwan-stock-predictor
python data_manager.py
# 訪問: http://localhost:8081
```

---

## 系統優勢

✅ **無依賴**: 不需要 Yahoo Finance、網路或 API key
✅ **準確**: 所有數據由您控制和驗證
✅ **快速**: 本地數據讀取，零延遲
✅ **可靠**: 無須擔心第三方服務宕機
✅ **簡單**: 簡單的 JSON 格式，易於管理

---

## 需要幫助？

如果遇到問題，請檢查：
1. 兩個服務器是否都正常運行
2. `stock_data.json` 是否存在且格式正確
3. 瀏覽器是否訪問了正確的 URL

祝您使用愉快！🚀
